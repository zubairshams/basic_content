<%= f.fields_for :contentable, contentable do |c| %>
  <%= c.hidden_field :type %>
  <%= c.hidden_field :multilingual_page_id %>
  <%= c.hidden_field :id %>

  <div>
    <%= c.label :url, t('content_types.url') + '*' %>
    <%= c.url_field :url %>
  </div>
<% end %>
